export enum FieldType {
    String,
    Number,
    Boolean,
    Coordinates
}

export enum InputType {
    None,
    Hidden,
    Text,
    Textarea,
    Checkbox,
    Map,
    Select,
    Entity,
    EntityCollection
}