export enum FieldType {
    String,
    Number,
    Date,
    Boolean,
    Coordinates,
    Entity,
    EntityCollection
}

export enum InputType {
    None,
    Hidden,
    Date,
    Text,
    Textarea,
    Checkbox,
    Map,
    Select,
    Entity,
    EntityCollection
    // TODO: Boolean, File, Image
}