export enum FieldType {
    String,
    Number,
    Time,
    Date,
    Array,
    DateTime,
    Boolean,
    Coordinates,
    Entity,
    EntityCollection
}

export enum InputType {
    None,
    Hidden,
    Static,
    Text,
    Textarea,
    Checkbox,
    Select,
    Map,
    Entity,
    EntityCollection
    // TODO: Map, File, Image
}